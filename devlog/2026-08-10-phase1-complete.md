# 2026-08-10 フェーズ1（AC1〜AC2）達成

6回目の実機実行で、トンネル URL の発行 → 別タブで Open WebUI を開く → 管理者アカウント作成 →
qwen3:8b との日本語チャットまで通しで確認できた。

## 確認できたこと

- AC1: 上から実行するだけでトンネル URL が出る（トンネル取得は1回で成功、5秒）
- AC2: URL を別タブで開き、モデルと会話できる
- リスク1（Python 3.11 venv）: 解消。venv 上で Open WebUI v0.11.0 が正常に動作
- リスク3（Quick Tunnel）: リトライ＋疎通確認を実装済み。実測では1回で取得できた
- リスク4（WebSocket）: 解消。ストリーミング表示が cloudflared 経由で動作

## 観測メモ

- 初回メッセージの応答に約3分。これはモデル（約5GB）をディスクから GPU へロードする時間で、
  セッション内で最初の1回だけ発生する。UI の「4秒間の思考」表示は思考トークン分のみで、
  ロード時間は含まれない
- クライアント側の DNS の罠（発行直後アクセスによるネガティブキャッシュ）は、ブラウザの
  セキュア DNS（DoH, Cloudflare）を有効にして回避した。ノートブック側の疎通確認により今後は再発しない想定
- 各セルの所要時間の詳細ログは未収集。次回の起動で AC6 の基準値として記録する

## 残タスク

- AC3（ランタイム削除後の履歴復元）/ AC4（停止セルでの書き戻し）→ フェーズ3で実装、次回検証
- AC6（起動時間）の実測、モデル置き場（Drive vs 毎回 pull）の比較 → フェーズ4
