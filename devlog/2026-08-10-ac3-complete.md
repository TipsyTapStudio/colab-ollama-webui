# 2026-08-10 AC3/AC4 達成 — 本丸クリア

Drive 永続化の実装後、通しで検証が完了した。

1. 稼働中のランタイムで再実行 → ローカル優先の復元スキップが機能し、既存の会話を保持したまま
   Drive マウント＋バックアップが有効化された
2. 停止セル（STOP チェック）→「履歴を Drive へ書き戻した」を確認（**AC4**）
3. ランタイムを削除 → まっさらな VM で再実行 → セル2で Drive から復元 → 前回のメール＋
   パスワードでログイン → **前回の会話がチャット一覧に残っており、続きから話せた（AC3）**

リスク2（SQLite × Drive FUSE）も、この方式（ローカル実行＋スナップショット書き戻し）で
問題が出ないことを確認。PRD の6つのリスクはすべて解消した。

## 実測メモ（AC6 用）

- `pip install open-webui`: 303秒（まっさらな VM での2回目起動時）。起動時間の支配項
- 初回メッセージの応答: 約3分（モデルの GPU ロード。セッション内で1回だけ）

## ユーザーフィードバックによる改善

1. **長い無出力コマンドの経過表示**: `pip install -q` の5分間が「止まっているように見える」
   → `run()` に heartbeat オプションを追加し、30秒ごとに経過時間を表示するようにした
2. **初回アクセス手順の簡素化**: New Sign Ups が初期値オフだったため、「サインアップ無効化」の
   操作手順を削除し、「まっさらな状態のときだけアカウントを作る」に書き直した

## 次（フェーズ4）

- AC6: 起動時間の内訳を実測し、目標値を確定する
- モデル置き場: Drive 配置 vs 毎回 pull を実測して速い方を採る（PRD 4章の未決事項）
- 起動短縮の候補: venv 構築結果のキャッシュ（pip の303秒を消せるか）
